// DATABASE LAYER (stub — Wave 1 me Prisma ayega).
// Steps: npm i prisma @prisma/client → .env me DATABASE_URL →
// npx prisma migrate dev → is file me: export const prisma = new PrismaClient()
// RULE: har query me `where: { storeId }` mandatory (tenant isolation).
export const prisma: any = null; // TODO(Wave 1)
